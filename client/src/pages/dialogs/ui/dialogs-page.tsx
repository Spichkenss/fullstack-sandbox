import { DialogsList } from "@/entities/dialog";

const DialogsPage = () => {
  return (
    <div style={{ marginTop: 30 }}>
      Список диалогов:
      <DialogsList />
    </div>
  );
};

export default DialogsPage;
